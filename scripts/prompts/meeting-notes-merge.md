You are writing the finished meeting notes for the {title} of {date_long}.

The transcript was too long for one pass, so it was split into portions and
each was extracted separately. Those extracts are below, in order. They are
your only record of what was said - work from them as you would from the
transcript itself.

Merging rules:

- The extracts overlap at the seams and an item may be split across two of
  them. Combine every mention of the same item into ONE row or paragraph. An
  extract marked *(continues past this portion)* is completed by the next one.
- Never drop an item because it appeared in only one extract. Every item across
  every extract must survive into the finished notes.
- Never invent connective detail to smooth a seam. If two extracts disagree, or
  an item is discussed but its outcome never recorded, say so inline -
  *(verify in minutes)*.
- Preserve every inline uncertainty marker the extracts carry.
- Order the finished notes by agenda order, not by the order the extracts
  happen to mention things.

Hard grounding rules:

- Every resolution/ordinance number you write must appear in the agenda or the
  extracts. If an item's number is not given, refer to it by subject only -
  never interpolate or guess a number.
- Every personal name must match the agenda's spelling when the person appears
  in the agenda; keep *(name per transcript)* markers from the extracts.
- Every dollar figure must appear verbatim in the extracts, the agenda, or the
  attachment excerpts.
- Vote splits and mover/seconder attributions must come from the extracts; if
  unclear, write *(verify in minutes)* in that item's row.
- Distinguish carefully between things that already happened and things
  expected to happen - do not recast past figures as projections.

Write a markdown document with exactly these sections, in this order:

1. Title line: `# Meeting Notes - Huntsville City Council <type>, <Month D, YYYY>`
2. Header block: called to order / adjourned times, presiding officer, members
   present, key officials present, then these two lines copied exactly, with no
   description of them:

   **Sources:** `transcript/whisper-medium.txt` (primary), `agenda.pdf`
   *(Draft generated by a local model from a chunked transcript; names and
   numbers normalized against the agenda. Whisper garbles proper nouns.)*
3. `## TL;DR` - 3-7 bullets: the outcomes a resident would care about most
   (what passed/failed/postponed/withdrawn, money, land use, controversy).
4. `## Decisions` - ONE table covering EVERY item acted on:
   `| Item | Subject | Outcome |`. Include resolution/ordinance numbers,
   appointee names and term end dates, dollar amounts, vote splits,
   abstentions, and postponement target dates. Items introduced without action
   get a one-line list after the table.
5. `## Discussion highlights` - bold-led paragraphs for the handful of items
   with real deliberation: who said what, concrete numbers, explicit asks made
   of the council, and staff commitments.
6. `## Public comments` - numbered list, one entry per speaker: name and the
   substance of their ask or complaint.
7. `## Watch list / follow-ups` - dated returns (postponed items, hearings set),
   unanswered asks, anything a future meeting should be checked against.

Flag uncertainty inline where it occurs - never in a separate caveats section.
If the extracts never record a vote the agenda expects, say so in that item's
table row. Do not invent anything not supported by the sources.

Output ONLY the markdown document.

<agenda>
{agenda}
</agenda>

<attachment_excerpts>
{attachments}
</attachment_excerpts>

<transcript_extracts>
{extracts}
</transcript_extracts>
